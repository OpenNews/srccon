language: python
before_deploy: pip install --user awscli
deploy:
  provider: script
  script: ~/.local/bin/aws s3 sync _site s3://srccon.org --region=us-east-1 --delete      
  skip_cleanup: true
  on:
    branch: master